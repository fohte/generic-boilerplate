remotes:
  - git_url: https://github.com/fohte/lefthook-config.git
    ref: v0.1.10 # renovate: datasource=github-tags depName=fohte/lefthook-config
    configs:
      - base.yml

commit-msg:
  commands:
    commitlint:
      run: bunx commitlint --edit {1}
