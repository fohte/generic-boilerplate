# Auto-generated files
CHANGELOG.md

# Copier rewraps this to a width prettier doesn't agree with, so the two
# fight over its formatting on every `copier update`.
.copier-answers.yml

# Snapshot fixtures rendered by scripts/generate-snapshots. Formatting them
# here would race with the regenerate-and-commit CI job's own commits to
# generated/, causing the two workflows to endlessly overwrite each other.
generated/
