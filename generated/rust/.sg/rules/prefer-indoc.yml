id: prefer-indoc
language: rust
severity: warning
message: |
  Use `indoc!` for multiline string literals to improve readability.
  The `indoc!` macro allows writing indented multiline strings without leading newlines.
note: |
  Replace with:
    indoc! {"
        your content here
    "}
rule:
  any:
    # raw string literal containing a newline (multiline)
    - kind: raw_string_literal
      regex: \n
      not:
        inside:
          kind: token_tree
    # normal string literal containing 2+ \\n escapes (truly multiline)
    # Exclude strings where lines have leading spaces (indoc would strip them)
    - kind: string_literal
      regex: "\\\\n.*\\\\n"
      not:
        any:
          - inside:
              kind: token_tree
          - regex: "\\\\n "
