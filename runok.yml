definitions:
  vars:
    project-scripts:
      type: path
      values:
        - ./scripts/apply-renovate-patch
        - ./scripts/check-renovate-diff-preserved
        - ./scripts/generate-snapshots
        - ./scripts/verify-sync-completeness
        - ./scripts/list-boilerplate-usage

rules:
  - allow: 'bash <var:project-scripts> *'
  - allow: '<var:project-scripts> *'
