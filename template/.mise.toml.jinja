[tools]
lefthook = "2.1.7"
{%- if has_rust %}
"github:ast-grep/ast-grep" = "0.42.2"
{%- endif %}

# lefthook hooks
actionlint = "1.7.12"
node = "24.15.0"
pinact = "3.10.1"
"github:fohte/basefmt" = "0.1.0"
{% if not has_node -%}
{#- Without a package.json, prettier and commitlint must be hosted by mise. -#}
"npm:@commitlint/cli" = "21.0.1"
"npm:prettier" = "3.8.3"
{% endif -%}
shellcheck = "0.11.0"
shfmt = "3.13.1"

{%- if has_rust %}

[env]
# Share target dir across worktrees to avoid full recompilation
CARGO_TARGET_DIR = "{% raw %}{{config_root}}{% endraw %}/target"
{%- endif %}
