# Auto-generated files
CHANGELOG.md

# Copier rewraps this to a width prettier doesn't agree with, so the two
# fight over its formatting on every `copier update`.
.copier-answers.yml
{% if has_node %}
# Lock files
pnpm-lock.yaml
{% endif -%}
{% if has_spa_pkg %}
# Generated by `msw init`; do not reformat. Its embedded PACKAGE_VERSION is
# not auto-synced with the msw devDependency version — regenerate with
# `npx msw init <spa-pkg>/public --save` after bumping msw.
mockServiceWorker.js
{% endif -%}
